<odoo>
  <data>
    <!-- explicit list view definition -->
    
    <record model="ir.ui.view" id="empleados_medicos">
      <field name="name">empleados list</field>
      <field name="model">empleados.empleados</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="id_card"/>
          <field name="birthyear"/>
          <field name="phone"/>
          <field name="charge"/>
          <field name="email"/>
          <field name="arrive_time"/>
          <field name="leave_time"/> 
          <field name="address"/>
          <field name="speciality"/>
          <field name="worked"/>
          <field name="pay"/>
        </tree>
      </field>
    </record>

    <record model="ir.ui.view" id="empleados_medicos_form">
      <field name="name">empleados form</field>
      <field name="model">empleados.empleados</field>
      <field name="arch" type="xml">
        <form>
          <sheet>
            <notebook>
              <page string="Personal Médico">
              </page>
              <page string="Personal de Servcio">
              </page>
              <page string="Personal Administrativo">
              </page>
            </notebook>
            <group>
              <field name="name"/>
              <field name="id_card"/>
              <field name="birthyear"/>
              <field name="phone"/>
              <field name="charge"/>
              <field name="email"/>
              <field name="arrive_time"/>
            </group>
            <group>
              <field name="leave_time"/>  
              <field name="speciality"/>
              <field name="address"/>
              <field name="worked"/>
              <field name="pay"/>
            </group>
          </sheet>
        </form>
      </field>
    </record>

    <!-- actions opening views on models -->
    
    <record model="ir.actions.act_window" id="empleados.action_medico">
      <field name="name">empleados window</field>
      <field name="res_model">empleados.empleados</field>
      <field name="view_ids" eval="[(5, 0, 0), 
        (0, 0, {'view_mode': 'tree', 'view_id': ref('empleados_medicos')}), 
        (0, 0, {'view_mode': 'form', 'view_id': ref('empleados_medicos_form')}),                           
        (0, 0, {'view_mode': 'kanban'})]"/>
      <field name="view_mode">tree,form</field>
    </record>

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="empleados.action_server">
      <field name="name">empleados server</field>
      <field name="model_id" ref="model_empleados_empleados"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    
    <menuitem name="empleados" id="empleados.menu_root"/>
   
    <!-- menu categories -->
    
    <menuitem name="Personal" id="empleados.menu_1" parent="empleados.menu_root" action="empleados.action_medico"/>

    <!--
    <menuitem name="Menu 2" id="empleados.menu_2" parent="empleados.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="empleados.menu_1_list" parent="empleados.menu_1"
              action="empleados.action_window"/>
    <menuitem name="Server to list" id="empleados" parent="empleados.menu_2"
              action="empleados.action_server"/>
    -->
  </data>
</odoo>